# 환경변수 (APP_KEY,SECRET,CANO...) 

# KIS API Credentials
APP_KEY=
APP_SECRET=

# KIS Account Info
CANO=
ACNT_PRDT=01

# KIS API Base URL
BASE_URL=https://openapi.koreainvestment.com:9443

# Discord Bot Token
DISCORD_TOKEN=

# Slack Webhook URL (for alerts)
SLACK_WEBHOOK=

# Database Configuration
DB_USER=user
DB_PASSWORD=password
DB_HOST=localhost # Use 'postgres' if running in Docker
DB_PORT=5432
DB_NAME=autotrade
DATABASE_URL=postgresql+psycopg2://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Qdrant Configuration
QDRANT_HOST=localhost # Use 'qdrant' if running in Docker
QDRANT_PORT=6333
QDRANT_URL=http://${QDRANT_HOST}:${QDRANT_PORT}

# Airflow Configuration (Optional - depends on setup)
# AIRFLOW_HOME=~/airflow
# AIRFLOW__CORE__EXECUTOR=LocalExecutor

# Grafana Configuration (Optional - for Docker setup)
# GRAFANA_USER=admin
# GRAFANA_PASSWORD=admin 